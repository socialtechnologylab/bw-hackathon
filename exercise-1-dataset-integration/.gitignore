# Your team identifier — never commit this.
.env

# Your produced artefacts — don't commit them.
data/*.zarr/
data/*.parquet
submission.tar.gz

# Stray downloads / caches an agent might create while wrangling GRIB.
*.grib
*.grib2
*.idx
*.index
.cache/
__pycache__/
.pytest_cache/
.venv/
